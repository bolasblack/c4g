{
  "name": "<%= name %>",
  "version": "0.1.0",
  "main": "lib/index.js",<% if (author) { %>
  "author": "<%= author %>",<% } %>
  "license": "MIT",
  "scripts": {
    "prepublishOnly": "yarn build",
    "build": "tsc --build .",
    "watch": "tsc --watch -p ."
  },
  "devDependencies": {},
  "dependencies": {}
}
