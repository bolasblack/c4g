{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsSchematicSchema",
  "title": "Schematic Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The package name for the new project."
    },
    "author": {
      "type": "string",
      "description": "Author for the new project."
    },
    "interactive": {
      "type": "boolean",
      "description": "Add project tool configs to the new project interactively.",
      "default": true
    }
  },
  "required": ["name"]
}
